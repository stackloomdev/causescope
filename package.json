{
  "name": "causescope-monorepo",
  "version": "1.0.0-beta.7",
  "private": true,
  "description": "Click any UI. Trace the cause.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/stackloomdev/causescope.git"
  },
  "packageManager": "pnpm@10.30.3",
  "type": "module",
  "scripts": {
    "dev": "pnpm build:packages && pnpm --filter @causescope/example-vite-react-19 dev",
    "dev:example": "pnpm build:packages && pnpm --filter @causescope/example-vite-react-19 dev",
    "docs:dev": "pnpm --filter @causescope/docs dev",
    "docs:build": "pnpm --filter @causescope/docs build",
    "build:packages": "turbo run build --filter=\"./packages/*\"",
    "build:packages:serial": "pnpm --filter \"./packages/*\" --workspace-concurrency=1 run build",
    "build": "turbo run build",
    "test": "turbo run test",
    "test:e2e": "pnpm build:packages && pnpm --filter @causescope/example-vite-react-19 test:e2e && pnpm --filter @causescope/example-vite-react-18 test:e2e && pnpm --filter @causescope/example-vite-react-swc test:e2e",
    "test:e2e:firefox": "pnpm build:packages && pnpm --filter @causescope/example-vite-react-19 test:e2e:firefox",
    "test:package": "tsx scripts/verify-package.ts",
    "snapshot:public-api": "pnpm build:packages && tsx scripts/verify-public-api.ts --write",
    "verify:links": "tsx scripts/verify-links.ts",
    "verify:performance": "tsx scripts/verify-performance.ts",
    "verify:public-api": "tsc --project scripts/tsconfig.json && tsx scripts/verify-public-api.ts",
    "verify:release": "tsc --project scripts/tsconfig.json && vitest run scripts/release-policy.test.ts && tsx scripts/verify-release.ts",
    "verify:production": "tsx scripts/verify-production.ts",
    "verify:stackblitz": "tsx scripts/verify-stackblitz.ts",
    "verify:trace-contract": "tsc --project scripts/tsconfig.json && tsx scripts/verify-trace-contract.ts",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "check": "pnpm typecheck && pnpm test && pnpm build && pnpm verify:production && pnpm verify:performance && pnpm verify:public-api && pnpm verify:release && pnpm verify:links && pnpm verify:trace-contract && pnpm verify:stackblitz && pnpm test:package && pnpm test:e2e"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "esbuild": "^0.28.1",
    "tsup": "^8.3.5",
    "tsx": "4.23.1",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2",
    "vitest": "^3.2.7"
  }
}
